// Access the HTML element with the ID "helloWorldMessage"
const helloWorldMessage = document.getElementById('helloWorldMessage');

// Update the content of the element with the "Hello World" message
helloWorldMessage.textContent = 'Hello World!';
